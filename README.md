# Webcraft AI

Protótipo para geração de interfaces web (HTML/CSS/JS) a partir de comandos em linguagem natural, com representação intermediária em JSON.

## Requisitos
- Node.js + npm
- Chave de API do provedor (OPENAI_API_KEY)

## Execução local
1) Instale dependências:
```bash
npm install